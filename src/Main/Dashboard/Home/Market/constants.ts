import type { DashboardFilters } from "./types"

export const DEFAULT_DASHBOARD_FILTERS: DashboardFilters = {
  dateRangePreset: "30d",
  created_at_after: "",
  created_at_before: "",
  market: "",
}
